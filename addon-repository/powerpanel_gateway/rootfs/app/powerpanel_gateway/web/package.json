{
  "name": "powerpanel-gateway-web",
  "version": "0.1.0",
  "private": true,
  "description": "Optional Vite scaffold for a richer PowerPanel Gateway UI. The backend serves the dependency-free static UI (index.html/app.js/styles.css) by default; this build step is not required.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
